
<div>
  <% flash.each do |key, value| %>
  <%= content_tag(:div, value, class: "#{key}") %>
  <% end %>
</div>

<div>
*.rb, *.txt, *.html のようなテキストファイルを選択してください。
<br>
</div>

<%= form_tag read_confirm_path, multipart: true do %>
<%= file_field_tag :file %>
<%= submit_tag "読み込み" %>
<% end %>

<%= form_tag read_confirm_path do %>
<div class="field">
  <%= text_field_tag 'read[name]' %>
</div>
<div class="actions">
  <%= submit_tag %>
</div>
<% end %>
